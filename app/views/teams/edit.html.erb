<h1>Editing team</h1>

<%= render 'form' %>

<%= link_to "show the #{@team.name} team", @team %> |
<%= link_to "back to teams", teams_path %>

<br/><br/>
<%= link_to "add project", :controller => 'projects', :action => 'new', :team => @team.id, :class => "button" %>

